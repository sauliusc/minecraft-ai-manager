import { Router } from 'express';

export const playersRouter = Router();

// POST   /api/players      — implemented in #10
// GET    /api/players      — implemented in #10
// GET    /api/players/:id  — implemented in #10
// PATCH  /api/players/:id  — implemented in #10
